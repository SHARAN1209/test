// Save MultipartFile temporarily
File convFile = File.createTempFile("upload-", file.getOriginalFilename());
file.transferTo(convFile);

// Build body
MultiValueMap<String, Object> body = new LinkedMultiValueMap<>();
body.add("file", new FileSystemResource(convFile)); // ✅ important: use FileSystemResource
body.add("groups", String.join(",", groups));
body.add("currentUser", currentUser);
